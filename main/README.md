# Script guide

These are the scripts kept as the current core-method entry points.

- `functions.R`: core proposed methods and simulation helpers.
- `real_data_pipeline.R`: real-data fitting pipeline for the PROMIS / SRPPER application.
- `simulation.R`: simulation driver that writes raw summary files.